The resources and solutions folders are separate Vue.js projects.

Once extracted from the Zip file, you have to run the following commands in the console from within each Vue.js project folder:
npm install
npm run dev
